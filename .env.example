# Read by docker-compose.yml itself (not by any single service) to pass
# build args through. Copy to .env and fill in - same value as
# client/.env's VITE_GOOGLE_CLIENT_ID and server/.env's GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
