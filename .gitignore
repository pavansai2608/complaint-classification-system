# Node
node_modules/
dist/
npm-debug.log*

# Python
__pycache__/
*.pyc
.venv/
.pybuilder/
target/

# Environment files (keep .env.example)
.env
.env.*
!.env.example

# Real Kubernetes secret values (keep secret.example.yaml)
k8s/secret.yaml

# OS
.DS_Store

# Local MCP server config - contains no secrets (key comes from an env var)
# but stays local so it does not affect anyone else working on this repo.
.mcp.json
.claude/launch.json
