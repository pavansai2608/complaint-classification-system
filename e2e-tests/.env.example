# Where the React app is running
CLIENT_BASE_URL=http://localhost:5173

# Where the Express API is running (used to check it is up before the tests start)
SERVER_BASE_URL=http://localhost:4000

# Set to false to watch the browser while a test runs
E2E_HEADLESS=true

# Seconds to wait for a page or element before a test fails. Submitting a
# complaint waits on a real AI analysis call, which can take 10-15s cold.
E2E_TIMEOUT=30
